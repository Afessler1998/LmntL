{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-g",
                "-o",
                "LmntL",
                "${workspaceFolder}/src/main.c",
                "-I${workspaceFolder}/libraries/include",
                "-L${workspaceFolder}/libraries/lib",
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}